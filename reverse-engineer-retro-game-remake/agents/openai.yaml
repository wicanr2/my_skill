interface:
  display_name: "Retro Game RE + Remake"
  short_description: "Fingerprint toolchains and build evidence-backed clean remakes"
  default_prompt: "Use $reverse-engineer-retro-game-remake to fingerprint the original toolchain, separate runtime from product code, approve a READY spec, and verify a clean remake."
